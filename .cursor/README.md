## Cursor Guidance

- Do not commit or push code to GitHub — that is for humans only.
- Any documentation written for planning or thinking must go into `docs/chatter/`.

This repository includes CI that runs tests on pull requests targeting `main`. Keep ephemeral notes in `docs/chatter/` and formal docs/ADRs in `docs/`.
